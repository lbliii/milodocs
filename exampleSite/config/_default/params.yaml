# Example site parameters
# Copy relevant sections to your main config
logo: '/images/milo.png'
names:
  organization: 'Example Org'
  product: 'Example Product'
version: 
  major: 0
  minor: 0
  patch: 3
article:
  childArticles:
    display: true
    count: 7 
    titleTrunc: 90
    descTrunc: 90
  nextPrev: 
    display: true
    trunc: 35
  chat: 
    display: true
  # Sidebar TOC (right sidebar)
  toc: 
    display: true
  # Inline components (within article content)
  inlineToc:
    display: false
    showProgress: false
  pageResources:
    display: true
  # Content status warnings (draft and expiry alerts)
  statusWarnings:
    display: true
  # Enhanced metadata section (dates, author, reading time, word count)
  metadata:
    display: true
  # Related content section (shows related pages in same section)
  relatedContent:
    display: true

# Default notebook repository configuration
notebook:
  # Default repository URL for notebooks (can be overridden per page)
  defaultRepoUrl: "https://github.com/nvidia/notebook-examples"
  # Default branch (can be overridden per page)
  defaultBranch: "main"

# Development & Debug Settings
debug: true  # Enable debugging to see what parameters are loaded

# Performance & Build Settings
useHugoNativeCSS: true  # Hugo native Tailwind CSS processing (Tailwind 4.x)

# Theme performance features
performance:
  # Enable partial caching for navigation components
  cacheNavigation: true
  
  # Use build stats for CSS optimization
  useBuildStats: true
  
  # Preload critical resources
  preloadFonts: true