{
  "properties": {
    "amount": { "$ref": "#/components/schemas/amount_view" },
    "id": { "$ref": "#/components/schemas/capture_id" },
    "merchant_event_id": {
      "$ref": "#/components/schemas/credit_card_void/properties/merchant_event_id"
    },
    "metadata": {
      "additionalProperties": {
        "example": { "key1": "value1", "key2": "value2" },
        "type": "string"
      },
      "type": "object"
    },
    "splits": { "$ref": "#/components/schemas/transaction_splits_view" },
    "status": { "$ref": "#/components/schemas/capture_status" }
  },
  "type": "object"
}
